# Documentación

>>Métodos y Más

    sum( : "Suma números (no admite variables)".
    sub( : "Resta números (no admite variables)".
    print( : "Imprime cualquier cosa en pantalla (no admite variables)".
    mul( : "Multiplica números (no admite variables)".
    mulVarInt( : "Multiplica valores enteros almacenados en variables".
    sumVarInt( : "Suma valores enteros almacenados en variables".
    div( : "Divide valores double (no admite variables)".
    >> : "La siguiente linea se realiza la declaración de la variable entera (NO LLEVA ';' )".
    << : "La siguiente linea se realiza la declaración de la variable String (NO LLEVA ';' )".

>>Importante

	* RECUERDE CERRAR EL MÉTODO.
	* EJEMPLO:

		init;

		sum(
		5+9
		)

		end;

	*LOS ARCHIVOS LASCRIPT LLEVAN LA EXTENCIÓN (.las).

>>Ventajas
  
  -Fácil de usar.
  -No necesitas un IDE para usarlo, solo basta el bloc de notas.

>>Desventajas

  -Es bastante básico.
  
